fastapi==0.116.1
uvicorn[standard]==0.35.0
sqlalchemy==2.0.43
pydantic==2.11.9
pydantic-settings==2.10.1
email-validator==2.2.0
python-jose[cryptography]==3.4.0
passlib==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20
httpx==0.28.1
anthropic==0.69.0

# Postgres driver - needed now that we've moved off local SQLite to Supabase
psycopg2-binary==2.9.13

# Local LLM client - talks to a local Ollama server (no API key needed)
ollama

# Model Context Protocol SDK.
# IMPORTANT: pin to the 1.x line. mcp 2.0 renamed FastMCP -> MCPServer and
# changed several other APIs; this codebase targets the 1.x FastMCP API.
mcp<2

pytest==8.4.1
